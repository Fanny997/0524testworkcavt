/**
 * @param {Root} tree
 * @param {Options} options
 * @returns {Map<string, Anchor>}
 */
export function getAnchors(tree: Root, options: Options): Map<string, Anchor>;
export type Anchor = {
    systemId?: Element;
    systemName?: Element;
    userId?: Element;
    userName?: Element;
};
export type Options = {
    /**
     *  Accept `user-content-` prefix on elements.
     */
    resolveClobberPrefix: boolean;
};
import type { Root } from 'hast';
import type { Element } from 'hast';
//# sourceMappingURL=anchors.d.ts.map